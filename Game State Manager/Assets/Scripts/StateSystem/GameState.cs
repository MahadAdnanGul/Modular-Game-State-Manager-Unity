namespace StateSystem
{
    /// <summary>
    /// Game State enum, add your own if needed
    /// </summary>
    public enum EGameState
    {
        LOADING,
        MENU,
        PREGAME,
        MAINLOOP,
        VICTORY,
        GAMEOVER,
    }
}
